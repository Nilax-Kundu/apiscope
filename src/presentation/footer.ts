/**
 * Explicit Non-Goals Echo for V3
 * Footer stating what the tool does NOT infer
 * 
 * INVARIANT: Preempts misuse through explicit boundaries
 */

/**
 * Non-goals footer to appear in all outputs
 * Sets explicit expectations about tool limitations
 */
export const NON_GOALS_FOOTER = `
Note: This report does not infer causality, ownership, or impact.
It reports observed behavior with uncertainty and context.
`.trim();
